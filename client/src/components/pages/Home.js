import React, { Fragment } from "react";

import RegisterForm from "../auth/RegisterForm";
const Home = () => {
  return (
    <Fragment>
      <div>{/* <RegisterForm /> */}</div>
    </Fragment>
  );
};

export default Home;
